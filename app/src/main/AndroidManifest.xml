<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.workmate">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@drawable/workmate_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/workmate_logo_circle"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorkMate">
        <activity android:name=".ListOfMessages"></activity>

        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".ListOfMessageActivity"
            android:label="@string/title_activity_list_of_message">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MessagesActivityTemp" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".Activities.ServiceRegActivity"
            android:parentActivityName=".Login" />
        <activity
            android:name=".Activities.ClientRegActivity"
            android:parentActivityName=".Login" />
        <activity
            android:name=".Activities.EditProfileActivity"
            android:parentActivityName=".Activities.MainActivity" />
        <activity
            android:name=".Login"
            android:parentActivityName=".Activities.MainActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>